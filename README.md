# iot-smart-parking-sytem
smart parking management system involving esp32 wifi microcontroller,data transmission is primarily via mqtt to mqtt broker(mosquitto),a python bridge that subscribes to mqtt broker  and updates the realtime database created using firebase,a web app hosted via  firebase showing slot demonstration
